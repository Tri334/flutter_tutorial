import 'package:flutter/material.dart';

class LatihanAppBar extends StatelessWidget {
  const LatihanAppBar({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
